# Biomass UCVME (clean copy)

## File mapping
- `train.py` <- copied from `dino_lb_ucvme.py`
- `inference.py` <- copied from `dino_inference_v2.py` (Kaggle-matching inference)

`dino_lb_ucvme_infer_latest.py` is intentionally not used in this clean folder.

## Config
Both scripts auto-load `.env` from this folder on startup.

## Run
```bash
cd /home/matt/Documents/image2biomass/scripts/biomass_ucvme
python3 inference.py
```
