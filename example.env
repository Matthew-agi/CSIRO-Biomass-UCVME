# Paths shared by train.py and inference.py
# Update these for your machine.

CSIRO_BIOMASS_DIR=/path/to/csiro-biomass
CSIRO_TRAIN_CSV=/path/to/csiro-biomass-entry/train.csv
CSIRO_TRAIN_IMAGE_DIR=/path/to/csiro-biomass-entry/train
CSIRO_TEST_IMAGE_DIR=/path/to/csiro-biomass/test
CSIRO_TEST_CSV=/path/to/csiro-biomass/test.csv
CSIRO_SUBMISSION_DIR=/path/to/output

# Fold checkpoints directory (best_model_fold*.pth files)
CSIRO_MODEL_DIR=/path/to/csiro-ulb2-folds

# Optional: path to a backbone checkpoint file
CSIRO_BACKBONE_PATH=/path/to/vit_huge_plus_patch16_dinov3.lvd1689m_backbone.pth

# Optional: override model architecture string
CSIRO_MODEL_NAME=vit_huge_plus_patch16_dinov3.lvd1689m

# Optional extras used by train.py
VISTAMILK_DIR=/path/to/vistamilk
VEGANN_DIR=/path/to/VegAnn_dataset
BAD_MARKS_CSV=/path/to/cluster_bad_marks.csv
